/**
 * Loading component for the Kindness Map page.
 * This component is displayed while the main content of the Kindness Map is being loaded.
 * It's part of Next.js's loading UI convention, automatically shown during page transitions
 * or when data fetching is in progress.
 *
 * @returns {JSX.Element} A simple loading indicator
 */
export default function Loading() {
	return (
		<div>Loading</div>
	)
}